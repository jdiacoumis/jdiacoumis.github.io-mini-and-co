# Local development configuration (mock/console drivers — contains NO secrets).
# Never put real API keys in this tracked file. For local Stripe TEST keys use
# `wrangler secret put` or an untracked .dev.vars override per DEPLOYMENT.md.
ENVIRONMENT=development
EMAIL_DRIVER=console
PAYMENTS_DRIVER=mock
PUBLIC_BASE_URL=http://localhost:8787
ADMIN_EMAILS=miniandco.classes@gmail.com
